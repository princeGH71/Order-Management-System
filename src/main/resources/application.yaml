spring:
  application:
    name: order-management-api
    
  datasource:
    url: jdbc:postgresql://localhost:5432/orderdb
    username: postgres
    password: 1909
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis
    redis:
      time-to-live: 600000 # 10 minutes in milliseconds
      
      
server:
  port: 8085
  
jwt:
  secret: "jwfasjncifknjwncdcnwqwcdwmcWNCNDHSACWNKJFEUVSDF"

  